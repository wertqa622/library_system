<Window x:Class="library_management_system.View.Loan_Book"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:library_management_system"
            xmlns:mdi="http://materialdesigninxaml.net/winfx/xaml/themes"
            WindowStyle="None"
            mc:Ignorable="d"
            Title="도서 정보"
            Height="600"
            Width="1000"
            WindowStartupLocation="CenterOwner"
            ResizeMode="NoResize"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            Background="White"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextElement.FontFamily="{StaticResource DefaultFontFamily}"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Loaded="Window_Loaded">

    <Window.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Teal.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.DatePicker.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <local:ImagePathConverter x:Key="ImagePathConverter" />
            <!-- 바이트[] -> BitmapImage 컨버터 등록 -->
            <local:ImageByteArrayConverter x:Key="ImageByteArrayConverter" />
            <!--디자인-->
            <Style x:Key="CustomDataGridStyle" TargetType="DataGrid">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#E0E0E0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="GridLinesVisibility" Value="Horizontal" />
                <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0" />
                <Setter Property="RowBackground" Value="White" />
                <Setter Property="AlternatingRowBackground" Value="#FAFAFA" />
                <Setter Property="SelectionMode" Value="Single" />
                <Setter Property="CanUserResizeRows" Value="False" />
                <Setter Property="CanUserSortColumns" Value="True" />
                <Setter Property="AutoGenerateColumns" Value="False" />
                <Setter Property="HeadersVisibility" Value="Column" />
                <Setter Property="RowHeight" Value="48" />
                <Setter Property="ColumnHeaderHeight" Value="56" />
                <Setter Property="SelectionUnit" Value="FullRow" />
            </Style>

            <Style x:Key="CustomDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#F5F5F5" />
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="CustomDataGridTextColumnStyle" TargetType="TextBlock">
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <mdi:Card Grid.Row="0" Padding="16" Margin="0,0,0,20" mdi:ElevationAssist.Elevation="Dp2">
            <StackPanel Orientation="Horizontal">
                <mdi:PackIcon Kind="BookOpen" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,16,0" />
                <TextBlock Text="반납도서" FontSize="20" FontWeight="Medium" VerticalAlignment="Center" />
            </StackPanel>
        </mdi:Card>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBox x:Name="SearchTextBox" Width="400" Margin="0,0,10,0"
                 mdi:HintAssist.Hint="대출한 도서명, 저자, 출판사로 검색"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 VerticalAlignment="Center" />
            <Button Content="검색" Width="100" Click="SearchButton_Click" Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
        <!-- Book Information DataGrid -->
        <DataGrid x:Name="LoanBooksGrid"
                  Grid.Row="2"
                  ItemsSource="{Binding Books}"
                  SelectedItem="{Binding SelectedBook}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  Style="{DynamicResource CustomDataGridStyle}"
                  ColumnHeaderStyle="{DynamicResource CustomDataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="사진" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="1" Margin="2">
                                <!-- 바이트[] 바인딩 시 컨버터 필요 -->
                                <Image Source="{Binding BookImage, Converter={StaticResource ImageByteArrayConverter}}"
                                       Height="60"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="ISBN"
                                                        Binding="{Binding Isbn}"
                                                        Width="180"
                                                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="도서명"
                                                        Binding="{Binding BookName}"
                                                        Width="150"
                                                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="저자"
                                                        Binding="{Binding Author}"
                                                        Width="100"
                                                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="대여일"
                                                        Binding="{Binding LoanDate, StringFormat={}{0:yyyy-MM-dd}}"
                                                        Width="150"
                                                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="반납예정일"
                                                        Binding="{Binding DueDate, StringFormat={}{0:yyyy-MM-dd}}"
                                                        Width="150"
                                                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTemplateColumn Header="반납" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="반납"
                                    Style="{DynamicResource MaterialDesignRaisedButton}"
                                    Margin="10,10,10,10"
                                    VerticalAlignment="Center"
                                    Click="ReturnBook_Click"
                                    ToolTip="반납">
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <mdi:Card Grid.Row="3" Padding="16" Margin="0,20,0,0" mdi:ElevationAssist.Elevation="Dp2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="닫기"
                        Style="{DynamicResource MaterialDesignRaisedButton}"
                        Click="loanbook_close"
                        Width="100"
                        Height="40" />
            </StackPanel>
        </mdi:Card>
    </Grid>
</Window>