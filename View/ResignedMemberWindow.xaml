<Window x:Class="library_management_system.View.ResignedMemberWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:library_management_system"
    xmlns:mdi="http://materialdesigninxaml.net/winfx/xaml/themes"
    WindowStyle="None"
    mc:Ignorable="d"
    Title="새 도서 추가" 
    Height="530" 
    Width="1000"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    Background="White"
    TextElement.FontWeight="Regular"
    TextElement.FontSize="13"
    TextElement.FontFamily="{StaticResource DefaultFontFamily}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto">

    <Window.Resources>
        <ResourceDictionary>
            <local:ImagePathConverter x:Key="ImagePathConverter"/>

            <Style x:Key="CustomDataGridStyle" TargetType="DataGrid">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#E0E0E0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="GridLinesVisibility" Value="Horizontal" />
                <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0" />
                <Setter Property="RowBackground" Value="White" />
                <Setter Property="AlternatingRowBackground" Value="#FAFAFA" />
                <Setter Property="SelectionMode" Value="Single" />
                <Setter Property="CanUserResizeRows" Value="False" />
                <Setter Property="CanUserSortColumns" Value="True" />
                <Setter Property="AutoGenerateColumns" Value="False" />
                <Setter Property="HeadersVisibility" Value="Column" />
                <Setter Property="RowHeight" Value="48" />
                <Setter Property="ColumnHeaderHeight" Value="56" />
                <Setter Property="SelectionUnit" Value="FullRow" />
            </Style>

            <Style x:Key="CustomDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#F5F5F5" />
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>
            <Style x:Key="CustomDataGridCellStyle" TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="12,8" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Background="{TemplateBinding Background}"
                         BorderBrush="{TemplateBinding BorderBrush}"
                         BorderThickness="{TemplateBinding BorderThickness}"
                         Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="CustomDataGridTextColumnStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#212529" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                <Setter Property="Margin" Value="0" />
            </Style>
            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Teal.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.DatePicker.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
            </ResourceDictionary.MergedDictionaries>


        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="69*"/>
            <RowDefinition Height="369*"/>
            <RowDefinition Height="62*"/>
        </Grid.RowDefinitions>
        <mdi:Card Grid.Row="0" Padding="16" mdi:ElevationAssist.Elevation="Dp2" HorizontalAlignment="Center" Margin="0,-4,0,13" Width="241">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="178">
                <mdi:PackIcon Kind="AccountCircle" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,0,0"/>
                <TextBlock Text="탈퇴 회원 조회" FontSize="20" FontWeight="Medium" VerticalAlignment="Center"/>
            </StackPanel>
        </mdi:Card>
        <!-- 탈퇴 회원 조회 컬럼 -->

        <DataGrid Grid.Row="1"
          ItemsSource="{Binding ResignedMember}"
          SelectedItem="{Binding SelectedResignedMember}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          IsReadOnly="True"
          Style="{DynamicResource CustomDataGridStyle}"
          ColumnHeaderStyle="{DynamicResource CustomDataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="사진" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="1" Margin="2">
                                <Image Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}"
                               Height="60"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="이름"
                    Binding="{Binding Name}"
                    Width="100"
                    ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="생년월일"
                        Binding="{Binding brithdate}"
                        Width="150"
                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="이메일"
                        Binding="{Binding Email}"
                        Width="200"
                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="전화면호"
                        Binding="{Binding PhoneNumber}"
                        Width="150"
                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
                <DataGridTextColumn Header="성별"
                        Binding="{Binding gender}"
                        Width="100"
                        ElementStyle="{DynamicResource CustomDataGridTextColumnStyle}" />
            </DataGrid.Columns>
        </DataGrid>
        <mdi:Card Grid.Row="2" Padding="1" Margin="0,10,0,0" mdi:ElevationAssist.Elevation="Dp2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="닫기"
                Style="{DynamicResource MaterialDesignRaisedButton}"
                Click="ResignedMember_close"
                Width="100"
                Height="40"/>
            </StackPanel>
        </mdi:Card>

    </Grid>
</Window>
